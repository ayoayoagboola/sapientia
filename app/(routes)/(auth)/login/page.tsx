"use client";

import { LoginForm } from "@/components/auth/LoginForm";

// TODO: improve login page... and form 

const LoginPage = () => {
  return (
    <div className="w-full h-full items-center justify-center flex flex-col bg-slate-50">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
